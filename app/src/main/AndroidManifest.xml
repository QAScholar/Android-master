<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vitalya.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Шпаргалка водителя"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="._1HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="._3RefuelingActivity" />
        <activity android:name="._4RepairsActivity" />
        <activity android:name="._2SelectionActivity" />
        <activity android:name="._5OilActivity" />
        <activity android:name="._6NotesActivity" />
        <activity android:name="._7StatsActivity"></activity>
    </application>

</manifest>